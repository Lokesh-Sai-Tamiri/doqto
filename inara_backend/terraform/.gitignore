# Terraform state — contains sensitive data, never commit
*.tfstate
*.tfstate.backup
.terraform/

# Your real variable values (contains secrets)
terraform.tfvars

# Crash logs
crash.log
crash.*.log

# Override files (local developer overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
